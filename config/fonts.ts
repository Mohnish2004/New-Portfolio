import { Manrope } from "next/font/google";

export const fontSans = Manrope({
  subsets: ["latin"],
  variable: "--font-sans",
});

export const fontMono = Manrope({
  subsets: ["latin"],
  variable: "--font-mono",
});
