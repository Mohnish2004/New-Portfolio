/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: [
        "api.microlink.io",
        "images.unsplash.com", // Microlink Image Preview
      ],
    },
  };
   
  module.exports = nextConfig;